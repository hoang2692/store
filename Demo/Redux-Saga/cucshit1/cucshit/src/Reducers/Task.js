const defaultState = {
  listTask: [
    {
      id:1,
      name:"phuong",
    }
  ],
};
const reducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default reducer;
